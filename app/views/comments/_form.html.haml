.comment-form
  %h5 コメントを投稿する
  = form_for [comment.article, comment], remote: true do |f|
    = render 'shared/error_messages', resource: comment do
      %p コメントの投稿に失敗しました

    .field
      .form-group
        = f.text_field :body, class: "form-control", placeholder: "コメントを入力してください..."
    .actions
      = f.submit '投稿', class: "btn btn-primary"
