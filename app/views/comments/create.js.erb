(function() {
  // add new comment to last
  document
    .querySelector(".comment-list")
    .insertAdjacentHTML("beforeend", "<%= j(render 'item', comment: @comment) %>");

  // reload form
  document.querySelector(".comment-form").outerHTML =
    "<%= j(render 'form', comment: @comment.article.comments.build) %>";
}());